import {SafeAreaView, StyleSheet, Text} from 'react-native';

import React from 'react';

export default function ExpandedPlayerBar() {
  return (
    <SafeAreaView>
      <Text>Expanded player</Text>
    </SafeAreaView>
  );
}
const styles = StyleSheet.create({});
